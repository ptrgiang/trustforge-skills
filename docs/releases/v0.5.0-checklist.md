# v0.5.0 Release Checklist

Status: **release candidate validated**

Target: `v0.5.0`

## Contract freeze

- [x] FreshPlan document schema remains `0.1`.
- [x] Refresh-request schema remains `0.1`.
- [x] Replacement-evidence schema remains `0.1`.
- [x] Plan-patch schema remains `0.1`.
- [x] Existing `ttl_seconds` / `valid_until` inputs remain supported.
- [x] Named freshness policies are additive.
- [x] `refresh_due` is non-invalidating; only `stale` invalidates dependent nodes.
- [x] `unknown` replacement change remains conservative and requires re-planning.
- [x] Future-dated observations remain fail-closed.

## Security / boundaries review

- [x] Freshness reports do not copy raw fact values.
- [x] Refresh requests do not copy raw fact values.
- [x] CLI does not dynamically import refresh adapters.
- [x] Adapter execution remains explicit trusted application code.
- [x] Adapter exceptions and missing registrations fail closed.
- [x] Wrong-fact replacement evidence is rejected.
- [x] Unknown replacement fields, including raw `value`, are rejected.
- [x] Cycles, self-dependencies, and unknown graph references are rejected.
- [x] Benchmark threshold is documented as a regression guard, not an SLA.

## Release artifacts

- [x] `docs/releases/v0.5.0.md` prepared.
- [x] `docs/releases/v0.5.0-checklist.md` prepared.
- [x] Package version finalized to `0.5.0`.
- [x] `trustforge.__version__` finalized to `0.5.0`.
- [x] CHANGELOG contains `[0.5.0] - 2026-09-11`.
- [x] README reflects v0.5.0 release snapshot.
- [x] ROADMAP marks release hardening complete except publish/post-release steps.

## Release-candidate validation

Validated on release-candidate snapshot `bbaa246ab5bea323a8d3fd8505cce35f13b72ea8` before this documentation-only closeout commit.

- [x] Python 3.10 CI passes.
- [x] Python 3.12 CI passes.
- [x] Python 3.13 CI passes.
- [x] SkillDiff action smoke passes.
- [x] DataLease regression gates pass.
- [x] FreshPlan selective-invalidation tests pass.
- [x] FreshPlan refresh/patch/adapter gates pass.
- [x] FreshPlan named-policy gate passes.
- [x] FreshPlan 10k-node performance gate passes.
- [x] Package/runtime version consistency reports `0.5.0`.
- [ ] Final documentation-closeout commit CI passes; use that exact SHA for the tag.

## GitHub release — manual

Do these only after the final documentation-closeout commit is green.

- [ ] Create tag `v0.5.0` from the exact final release SHA on `main`.
- [ ] Create GitHub Release named `TrustForge Skills v0.5.0`.
- [ ] Paste `docs/releases/v0.5.0.md` as release body.
- [ ] Mark as latest release.
- [ ] Do **not** mark as prerelease.
- [ ] Verify tag resolves to the exact release SHA.
- [ ] Verify Release is public and non-prerelease.

## Post-release

- [ ] Advance floating stable `v0` to the verified `v0.5.0` commit.
- [ ] Verify `uses: ptrgiang/trustforge-skills@v0` resolves successfully.
- [ ] Verify CI triggered by the stable-ref advance passes.
- [ ] Convert this checklist from release-candidate state to completed release record.
- [ ] Start `v0.6.0.dev0` development only after post-release verification.